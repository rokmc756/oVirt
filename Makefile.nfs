nfs:
	@if [ "${r}" = "init" ] || [ "${r}" = "uninit" ] || [ "${r}" = "reinit" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to init/uninit/reinit nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to init/uninit/reinit nfs";\
			fi\
		else\
			echo "No actions to init/uninit/reinit nfs";\
		fi;\
	elif [ "${r}" = "prepare" ] || [ "${r}" = "format" ] || [ "${r}" = "clean" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to prepare/format/clean nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to prepare/format/clean nfs";\
			fi\
		else\
			echo "No actions to prepare/format/clean nfs";\
		fi;\
	elif [ "${r}" = "create" ] || [ "${r}" = "recreate" ] || [ "${r}" = "copy" ] || [ "${r}" = "delete" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			elif [ ! -z ${c} ];  then\
				if [ "${c}" = "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{nfs_all: True}' --tags='${r}';\
				elif [ "${c}" != "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{"${c}": True}' --tags='${r}';\
				else\
					echo "No setup action";\
				fi\
			else\
				echo "No actions to create/copy/delete nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to create/copy/delete nfs";\
			fi\
		else\
			echo "No actions to create/copy/delete nfs";\
		fi;\
	elif [ "${r}" = "bootstrap" ] || [ "${r}" = "start" ] || [ "${r}" = "stop" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to bootstrap/start/stop nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to bootstrap/start/stop nfs";\
			fi\
		else\
			echo "No actions to bootstrap/start/stop nfs";\
		fi;\
	elif [ "${r}" = "enable" ] || [ "${r}" = "disable" ] || [ "${r}" = "config" ] || [ "${r}" = "change" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to enable/disable/config/change nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to enable/disable/config/change nfs";\
			fi\
		else\
			echo "No actions to enable/disable/config/change nfs";\
		fi;\
	elif [ "${r}" = "upload" ] || [ "${r}" = "download" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to upload/download nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to upload/download nfs";\
			fi\
		else\
			echo "No actions to upload/download nfs";\
		fi;\
	elif [ "${r}" = "install" ] || [ "${r}" = "uninstall" ] || [ "${r}" = "deploy" ] || [ "${r}" = "destroy" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to install/uninstall/deploy/destroy nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to install/uninstall/deploy/destroy nfs";\
			fi\
		else\
			echo "No actions to install/uninstall/deploy/destroy nfs";\
		fi;\
	elif [ "${r}" = "setup" ] || [ "${r}" = "add" ] || [ "${r}" = "remove" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			elif [ ! -z ${c} ];  then\
				if [ "${c}" = "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{nfs_all: True}' --tags='${r}';\
				elif [ "${c}" != "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{"${c}": True}' --tags='${r}';\
				else\
					echo "No Component";\
				fi\
			else\
					echo "No Aetup Action";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to add/setup/remove nfs";\
			fi\
		else\
			echo "No actions to add/setup/remove nfs";\
		fi;\
	elif [ "${r}" = "fetch" ] || [ "${r}" = "disfetch" ] || [ "${r}" = "rollback" ] || [ "${r}" = "return" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			elif [ ! -z ${c} ];  then\
				if [ "${c}" = "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{nfs_all: True}' --tags='${r}';\
				elif [ "${c}" != "all" ]; then\
					ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' -e '{"${c}": True}' --tags='${r}';\
				else\
					echo "No Component";\
				fi\
			else\
				echo "No actions to fetch/disfetch/rollback/return nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to fetch/disfetch/rollback/return nfs";\
			fi\
		else\
			echo "No actions to fetch/disfetch/rollback/return nfs";\
		fi;\
	elif [ "${r}" = "upgrade" ] || [ "${r}" = "downgrade" ]; then\
		if [ ! -z ${r} ] && [ "${s}" != "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{"${s}": True}' --tags='${r}';\
			else\
				echo "No actions to upgrade/downgrade nfs";\
			fi\
		elif [ ! -z ${r} ] && [ "${s}" = "all" ]; then\
			if [ -z ${c} ];  then\
				ansible-playbook --ssh-common-args='-o UserKnownHostsFile=./known_hosts' -u ${USERNAME} setup-nfs.yml -e '{nfs_all: True}' --tags='${r}';\
			else\
				echo "No actions to upgrade/downgrade nfs";\
			fi\
		else\
			echo "No actions to upgrade/downgrade nfs";\
		fi;\
	else\
		echo "No actions to nfs role";\
		exit;\
	fi

